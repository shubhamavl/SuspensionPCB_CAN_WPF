<Window x:Class="SuspensionPCB_CAN_WPF.Views.ManualADCEntryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Enter ADC Values" Height="200" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Enter ADC values for both modes:" 
                   FontSize="14" FontWeight="Bold" Margin="0,0,0,15"/>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Internal ADC (12-bit):" Width="150" VerticalAlignment="Center"/>
            <TextBox x:Name="InternalADCTxt" Width="100" Text="0" TextAlignment="Center"/>
        </StackPanel>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="ADS1115 ADC (16-bit):" Width="150" VerticalAlignment="Center"/>
            <TextBox x:Name="ADS1115ADCTxt" Width="100" Text="0" TextAlignment="Center"/>
        </StackPanel>
        
        <TextBlock Grid.Row="3" Text="Weight:" FontSize="12" Margin="0,0,0,5"/>
        <TextBlock Grid.Row="3" x:Name="WeightTxt" FontSize="14" FontWeight="Bold" 
                   Foreground="#FF1B5E96" Margin="150,0,0,5" VerticalAlignment="Center"/>
        
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="OK" Click="OK_Click" Width="80" Height="30" Margin="0,0,10,0" 
                    Background="#FF28A745" Foreground="White" BorderThickness="0"/>
            <Button Content="Cancel" Click="Cancel_Click" Width="80" Height="30" 
                    Background="#FFDC3545" Foreground="White" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>

